workflows:
  android-debug:
    name: Android Debug Build
    environment:
      java: 17

    scripts:
      - name: Build APK
        script: |
          gradle assembleDebug

    artifacts:
      - app/build/outputs/**/*.apk
